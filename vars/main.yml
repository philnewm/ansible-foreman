---

foreman_yum_url: "https://yum.theforeman.org/releases/{{ foreman_version }}/el9/x86_64/foreman-release.rpm"

foreman_ansible_python_packages:
  - pip
  - ansible
  - requests
  - psycopg2-binary

foreman_packages_command:
  RedHat: "rpm -qa | grep -i foreman"
  Debian: "dpkg -l | grep -i foreman"

...
