---

name: AlmaLinux9

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - 'README.md'
      - '**/*.md'
      - .gitignore
      - ./meta/main.yml
      - LICENSE

  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - 'README.md'
      - '**/*.md'
      - .gitignore

permissions:
  contents: read

jobs:
  alma9:
    uses: philnewm/ansible-role-repo-control/.github/workflows/molecule-ci-podman.yml@main
    with:
      scenario_name: almalinux9

...
